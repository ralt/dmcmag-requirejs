define( [ 'ui/accordions' ], function( accordions ) {
    // Function initializing the UI components
    function init() {
        accordions.init()
    }

    // Return the functions
    return {
        init: init
    }
} )

